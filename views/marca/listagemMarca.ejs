<!-- IMPORTAÇÃO DO HEADER -->
<%- include('../partials/header') %>

    <!-- IMPORTAÇÃO DO MENU -->
    <div class="container-fluid">
        <%- include('../partials/menu') %>
    </div>

    <div class="card">
        <div class="card-header">LISTAGEM DE MARCAS</div>
    </div>

    <% marcas.data.forEach(marca=>{ %>

        <div class="card mt-2 p-1">

            <div class="card-body">
                <%= marca.nome_marca %>
            </div>

            <div class="btn-group mr-2">
                <a href="/editarMarca/<%= marca.cod_marca %>" class="btn btn-warning">EDITAR</a>
                <a href="/excluirMarca/<%= marca.cod_marca %>" class="btn btn-danger">EXCLUIR</a>
            </div>

        </div>

        <% }) %>


<!-- IMPORTAÇÃO DO FOOTER -->
<%- include('../partials/footer') %>